# План рефакторинга EduHub

> После прохождения курса и перед деплоем привести проект в порядок

## 1. URL-маршруты (чистка дублей)
- [ ] Удалить дублирующиеся маршруты для курсов:
  - `course/<slug:course_slug>/` (оставить `courses/<slug:slug>/`)
  - `courses/<slug:slug>/` (или наоборот — выбрать один стандарт)
- [ ] Удалить `path('course/', views.courses_list, name='courses_list')` (дубль `courses/`)
- [ ] Удалить закомментированный `path('courses/<int:pk>/', ...)`
- [ ] Привести все URL к единому стилю (множественное число для списков)
- [ ] Переименовать `course_by_slug` в `course_detail` (или наоборот)

## 2. Неиспользуемые функции (мёртвый код)
- [ ] Проверить, какие функции-представления остались не у дел (после замены на CBV)
- [ ] Удалить `course_detail_by_slug` (если дублирует `course_detail`)
- [ ] Удалить старые функции после замены на классы:
  - `courses_list` → заменён на `CourseListView`
  - `home` → заменён на `HomeView`
  - `about` → заменён на `AboutView`
- [ ] Проверить `views.py` на наличие функций, которые нигде не используются

## 3. Структура приложений (выделение модулей)
- [ ] Создать отдельное приложение `courses/`:
  - Перенести модели, связанные с курсами
  - Перенести view для курсов
  - Перенести шаблоны `core/courses*.html` в `courses/templates/courses/`
  - Обновить импорты
- [ ] Создать отдельное приложение `teachers/`:
  - Перенести модель Teacher
  - Перенести view для преподавателей
- [ ] Создать отдельное приложение `students/` (или `pupils/`):
  - Перенести модель Student (если есть)
- [ ] Создать отдельное приложение `schedule/`:
  - Перенести модель Schedule и связанные view
- [ ] В `core/` оставить только общее:
  - Главная страница (HomeView)
  - AboutView
  - Контекстный процессор меню
  - Базовые шаблоны (`base.html`)
  - Статические файлы

## 4. Шаблоны (унификация)
- [ ] Привести все шаблоны к единому стилю:
  - Выбрать: Bootstrap или кастомные классы
  - Сейчас есть два варианта оформления курсов
- [ ] Переименовать шаблоны по конвенции Django:
  - `courses.html` → `course_list.html`
  - `course_detail.html` — уже ок
- [ ] Проверить, что все шаблоны используют одинаковые базовые блоки

## 5. Имена переменных в шаблонах
- [ ] Унифицировать имена переменных:
  - Где-то `courses`, где-то `course_list`
  - Привести к единому стандарту (через `context_object_name`)
- [ ] Проверить, что во всех шаблонах правильно используются пагинация и фильтры

## 6. Контекстные процессоры
- [ ] Убедиться, что меню работает через контекстный процессор
- [ ] Не дублировать `menu` в каждом view через `extra_context`

## 7. Миграции
- [ ] После выделения приложений — создать новые миграции
- [ ] Проверить, что данные не теряются при переносе
- [ ] В идеале — сделать дамп данных перед рефакторингом

## 8. Тестирование после рефакторинга
- [ ] Проверить все страницы сайта вручную
- [ ] Убедиться, что поиск работает
- [ ] Проверить все ссылки и формы
- [ ] Убедиться, что админка работает корректно

## 9. Дополнительно (по желанию)
- [ ] Переименовать `core` во что-то осмысленное (например, `common` или `main`)
- [ ] Добавить docstrings к основным классам и функциям
- [ ] Проверить настройки `settings.py` на наличие неиспользуемых переменных