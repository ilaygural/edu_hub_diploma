# WORKLOG — EduHub (ВКР, текущий этап)

> **Handoff:** после правок — `git commit` + `git push`; на другом ПК — `git pull`. История чата Cursor не переносится; ориентир — **этот файл** + `reports/vkr/`.

## 0) Быстрый старт с любого ПК (после `git pull`)

1. Ветка: **`prediploma-journal`**
2. `.venv` → `pip install -r requirements.txt`
3. `python manage.py migrate`
4. `python manage.py runserver`

## 1) Решения руководителя и сроки

- **Приоритет:** формальный **текст ВКР** к сдаче; **фичи в коде — минимум**, по остатку времени.
- **Деплой / отдельная глава тестов** — не в фокусе; проверка в **§3.6**, перспективы в **§3.7** и заключении.
- **Срок:** ~**2 недели** (текст + точечный код), с учётом основной работы.

## 2) Подход

1. Текст ВКР по блокам (введение → гл. 1–2 → гл. 3 → заключение).
2. Код — с **самой простой** фичи; в тексте честно: «реализовано» / «перспектива».
3. Перед сдачей: сверка текста с кодом и скриншотами (приложение А).

### Редактура текста

- Естественный инженерный тон, привязка к EduHub и Хобби-центру.
- Не завышать: «готовое к внедрению», «полный цикл», жёсткие формулировки по 152‑ФЗ.

## 3) Структура ВКР (в Word / `reports/vkr/структура ВКР.docx`)

```
ВВЕДЕНИЕ
1 АНАЛИЗ … (1.1–1.4)
2 ПРОЕКТИРОВАНИЕ … (2.1–2.6)
3 РЕАЛИЗАЦИЯ И АПРОБАЦИЯ … (3.1–3.7)
ЗАКЛЮЧЕНИЕ
```

Глава 3 в ВКР (не как в преддипломной): **3.2 серверная**, **3.3 клиент**, **3.4 бизнес-логика**, **3.5 роли**, **3.6 проверка**, **3.7 итоги**.

## 4) Сделано (сессии май 2026)

### Документация репозитория
- `README.md`, `PROJECT_SUMMARY.md`, `REFACTORING.md`, `WORKLOG.md`
- `reports/`: `tech/`, `prediploma/`, `vkr/`, `guidelines/`, `assets/`

### Текст ВКР (черновики в чате / перенос в Word)
- **Введение** — вычитано
- **Глава 1** — §1.1–1.4 (редакция под ВКР)
- **Глава 2** — §2.1–2.6 (в т.ч. Use Case, администратор = Admin + менеджерский контур)
- **Глава 3** — §3.1–3.7 + **заключение** (черновики для вставки в `reports/vkr/`)
- Преддипломная сдана (**5**)

### Код
- **КТП (педагог):** `/teacher/ktp/` — форма (период, дни недели, группа) → скачивание `.docx` (`core/ktp.py`, `python-docx`).
- Прототип: заявки → зачисление → журнал → кабинеты.

## 5) Следующий шаг (продолжить с другого ПК)

### A. Текст (если ещё не внесён в Word)
- Перенести вычитанные §1–3 и заключение в `reports/vkr/Гураль … черновик ВКР 2.docx`
- Сверить нумерацию рисунков/таблиц и список литературы
- В §3.7/заключении: **КТП** — обновить формулировку на «реализовано (каркас docx)»; МЗ и сообщения — перспектива, если не сделаны

### B. Код
**Сделано:** КТП. **Дальше:** полугодовые отчёты менеджера (Excel/CSV) → МЗ → по времени: сообщения / правка ребёнка / аудит.

**Проверка КТП:** `pip install -r requirements.txt` → вход как педагог → «Сформировать КТП» → период, дни недели → «Скачать документ».

## 6) Прототип (база)

- `users`, `accounts`, `core`, `schedule`; `manager/reports`, `manager/schedule`
- Одобрение заявки: профили + Enrollment + email
- SQLite; автотестов нет

## 7) Материалы

| Путь | Назначение |
|------|------------|
| `reports/vkr/` | Черновик ВКР, структура, варианты доработок |
| `reports/prediploma/` | Источник гл. 1–3 преддипломы |
| `reports/guidelines/` | ОС ТУСУР, рекомендации |

## 8) Ограничения разработки

- Без крупного рефакторинга — [REFACTORING.md](REFACTORING.md)
- Маленькие шаги: фича → проверка → commit → push

## 9) Git

```bash
git checkout prediploma-journal
git pull
# … работа …
git add WORKLOG.md reports/vkr/
git commit -m "docs: VKR chapter drafts and KTP feature plan in WORKLOG"
git push
```

Последний коммит handoff: `87ae855` — docs: VKR handoff. Следующий — текст ВКР в `reports/vkr/` + обновлённый WORKLOG.
