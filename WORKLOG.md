# WORKLOG — EduHub (преддиплом)

## 0) Быстрый старт с любого ПК (обязательно после git pull)
1. Переключиться на рабочую ветку:
   - `git checkout prediploma-journal`
   - `git pull`
2. Поднять окружение:
   - активировать `.venv`
3. Синхронизировать БД с кодом:
   - `python manage.py showmigrations accounts`
   - `python manage.py migrate`
4. Проверка запуска:
   - `python manage.py runserver`

## 1) Цель текущего этапа
Стабилизировать основной сценарий для преддипломной защиты:
- Родитель подает заявку
- Менеджер одобряет заявку
- Система создает Parent/Pupil/Enrollment
- Педагог ведет журнал (загрузка и сохранение посещаемости)
- Родитель видит данные в личном кабинете

## 2) Текущая рабочая ветка
prediploma-journal

## 3) Что уже сделано (факт)
- Журнал педагога работает штатно:
  - сохранение отметок посещаемости
  - повторная загрузка сохраненных отметок
  - изменение и пересохранение
- В менеджерском блоке заявок исправлена логика отображения действий по статусам
- Добавлен вывод сообщений (messages) в базовом шаблоне
- Исправлена ошибка TemplateDoesNotExist:
  - добавлен templates/core/includes/sidebar_parent.html
- В `Parent` добавлены поля для договора:
  - `address` (домашний адрес)
  - `passport` (паспортные данные)
- Добавлена миграция:
  - `accounts/migrations/0002_parent_contract_fields.py`

## 4) Что сейчас в работе
Профиль родителя для данных договора:
- минимальный набор полей для дозаполнения после первого входа
- без усложнения и без полной автоматизации договоров

## 5) Следующий микро-шаг (ближайший)
Довести форму редактирования профиля родителя до стабильного состояния:
- исправить `success_url` на имя маршрута:
  - `reverse_lazy('parent_dashboard')`
- редактировать ФИО родителя (из `User.first_name/last_name`) вместе с полями `Parent`
- не смешивать редактирование данных ребенка в этой форме

## 6) После следующего шага
- Проверить сценарий:
  - родитель открывает `/parent/profile/edit/`
  - сохраняет данные
  - видит success message
  - возвращается в dashboard без ошибок
- (Опционально) показать в форме список детей только для информации (без редактирования)

## 7) Ограничения до преддиплома
- Не делать крупный рефакторинг
- Не удалять массово “лишний” код
- Делать маленькими шагами: 1 изменение -> проверка -> коммит

## 8) Коммит-стиль
Conventional Commits:
- feat(...)
- fix(...)
- refactor(...)
- docs(...)

Примеры:
- feat(parent): add contract fields to parent profile
- feat(parent): add parent profile edit form
- fix(manager): show actions only for new applications

## 9) Быстрый чек перед пушем
- git status
- python manage.py showmigrations accounts
- python manage.py runserver (ручная проверка сценария)
- git add . && git commit -m "..."
- git push